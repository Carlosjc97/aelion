{
  "name": "aelion-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": ">=20 <=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test ./test/outline.test.mjs",
    "clean": "rimraf lib",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions:outline"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "overrides": {
    "undici-types": "6.20.0"
  }
}
