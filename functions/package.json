{
  "name": "aelion-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cpy \"data/**/*\" lib/data/",
    "test": "npm run build && node --test ./test/outline.test.mjs ./test/firestore.rules.test.mjs",
    "clean": "rimraf lib",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions:outline,functions:placementQuizStart,functions:placementQuizGrade"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "cpy-cli": "^6.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "overrides": {
    "undici-types": "6.20.0"
  }
}
