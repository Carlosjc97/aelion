{
  "name": "functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Aelion",
  "engines": {
    "node": "20"
  },
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions:outline"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}