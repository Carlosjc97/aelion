# Copy this template into your local .env and populate values via your secret manager.
CV_STUDIO_API_KEY=
AELION_ENV=development
BASE_URL=
POSTHOG_KEY=
POSTHOG_HOST=https://us.i.posthog.com
# Comma-separated list of current + previous HMAC keys. The first entry is active.
ASSESSMENT_HMAC_KEYS=
# Comma-separated list of allowed origins, required in every environment (no wildcard).
SERVER_ALLOWED_ORIGINS=
